package com.goreecloud.wallpapers.partner;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

/** Marker receiver discovered by WallpaperPicker2 for system partner resources. */
public final class PartnerCustomizationReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
        // Resource-only partner package. No runtime action is required.
    }
}
